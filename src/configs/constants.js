const USER_STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
};

const DEVICE_FROM = {
  COTIZADOR_API: 'cotizador-api',
};

const FACTURATECH_TYPE_USER = {
  CLIENTE_FACTURA: 'Cliente Factura',
  DISTRIBUIDOR_NORMAL: 'Distribuidor Normal',
  DISTRIBUIDOR_MASTER: 'Distribuidor Master',
  ADMINISTRADOR: 'Administrador',
};

const QUOTATION_REQUESTS_TYPES = {
  LINK: 'link',
  WS: 'ws',
};

const PAYMENT_METHODS = {
  WOMPI: 'Wompi',
};

const SALE_STATUS = {
  PENDING: 'pending',
};

const QUOTATION_REQUESTS_STATUS = {
  PENDING: 'pendiente',
  PROCESADA: 'procesada',
};

module.exports = {
  USER_STATUS,
  DEVICE_FROM,
  FACTURATECH_TYPE_USER,
  QUOTATION_REQUESTS_TYPES,
  PAYMENT_METHODS,
  SALE_STATUS,
  QUOTATION_REQUESTS_STATUS,
};
